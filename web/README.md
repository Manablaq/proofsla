# Frontend phase

Do not build the production UI until the Intelligent Contract passes lint, strict typecheck, schema generation, Direct Mode consensus tests, and hosted Studio integration.

Planned screens: Dashboard, Create SLA, SLA Detail, Evidence/Dispute, Verdict/Settlement.
